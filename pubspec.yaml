name: envelope
description: Envelope - A zero-based budgeting app
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  account_repository:
    path: packages/account_repository
  auth_repository:
    path: packages/auth_repository
  bloc: ^9.2.0
  budget_repository:
    path: packages/budget_repository
  connectivity_plus: ^6.1.4
  envelope_api_client:
    path: packages/envelope_api_client
  envelope_local_storage:
    path: packages/envelope_local_storage
  envelope_repository:
    path: packages/envelope_repository
  equatable: ^2.0.7
  firebase_core: ^3.12.1
  firebase_messaging: ^15.2.5
  fl_chart: ^0.70.2
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  go_router: ^15.1.2
  google_fonts: ^6.2.1
  goal_repository:
    path: packages/goal_repository
  intl: ^0.20.2
  notification_repository:
    path: packages/notification_repository
  package_info_plus: ^8.0.0
  path_provider: ^2.1.0
  provider: ^6.1.2
  report_repository:
    path: packages/report_repository
  share_plus: ^10.1.4
  shared_preferences: ^2.5.0
  sharing_repository:
    path: packages/sharing_repository
  subscription_repository:
    path: packages/subscription_repository
  supabase_flutter: ^2.12.2
  sync_repository:
    path: packages/sync_repository
  transaction_repository:
    path: packages/transaction_repository
  url_launcher: ^6.2.0
  uuid: ^4.5.1

dev_dependencies:
  bloc_lint: ^0.3.7
  bloc_test: ^10.0.0
  bloc_tools: ^0.1.0-dev.22
  drift: ^2.26.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.2.0

flutter:
  generate: true
  uses-material-design: true
