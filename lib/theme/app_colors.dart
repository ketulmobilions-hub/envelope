import 'package:flutter/material.dart';

/// App color constants for Envelope.
///
/// These are the seed colors used with Material 3 color scheme generation.
abstract final class AppColors {
  /// Primary seed color — green for financial/budgeting context.
  static const Color primarySeed = Color(0xFF2E7D32);
}
