# Supabase Edge Function secrets for local development.
# Copy this file to supabase/.env and fill in your values.
# supabase/.env is gitignored — never commit it.

RESEND_API_KEY=
FROM_EMAIL=Envelope <noreply@yourdomain.com>
